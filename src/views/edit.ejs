<!DOCTYPE html>
<html lang="es">
  <head>
    <meta charset="UTF-8" />
    <meta http-equiv="X-UA-Compatible" content="IE=edge" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <meta name="theme-color" content="#161b22" />
    <title>MD CheatSheets</title>
    <%- include('partials/head') %>
    <script src="https://cdn.jsdelivr.net/npm/marked/marked.min.js"></script>
    <style>
      * {
        box-sizing: border-box;
      }
      section {
        display: flex;
      }
      section div {
        width: 100%;
      }
      section div:first-child {
        padding-right: 20px;
      }
      section div input,
      section div textarea {
        width: 100%;
        margin-bottom: 20px;
        padding: 5px;
        box-sizing: border-box;
      }
      label {
        display: block;
      }
      textarea {
        height: 300px;
        resize: none;
      }
      section div:first-child {
        border-radius: 3px;
      }

      .container-preview {
        background-color: white;
        padding: 10px;
      }
      .markdown-body h1,
      h2,
      h3,
      h4,
      h5,
      h6 {
        color: #24292e;
        display: flex;
        align-items: center;
      }
      .titulo-preview {
        display: none;
      }
      @media screen and (max-width: 700px) {
        section {
          flex-direction: column;
        }
        section > div {
          margin-bottom: 30px;
        }
        .titulo-preview {
          display: block;
        }
        section div:first-child {
          padding-right: 0px;
        }
      }
    </style>
    <link rel="stylesheet" href="../css/md-mod.css" />
    <link rel="stylesheet" href="../css/hl/default.min.css" />
    <link rel="stylesheet" href="../css/hl/atom-one-dark.css" />
  </head>
  <body>
    <%- include('partials/header') %>
    <div class="container contenido" style="max-width: 98% !important">
      <h1>Editar Documento</h1>
      <div class="">
        <section>
          <div>
            <label class="d-block" for="titulo">Titulo</label>
            <input type="text" id="titulo" value="<%= titulo %>" />
            <label class="d-block" for="descripcion">Descripcion</label>
            <input type="text" id="descripcion" value="<%= descripcion %>" />
            <label for="contenido">Contenido</label>
            <textarea id="contenido"><%= contenido %></textarea>
            <a
              href="#"
              id="actualizarDocumento"
              class="btn verde text-blanco w-100 text-center"
              >Actualizar</a
            >
          </div>
          <h1 class="titulo-preview">Preview</h1>
          <div class="container-preview">
            <div id="preview" class="markdown-body"></div>
          </div>
        </section>
      </div>
    </div>

    <%- include('partials/scripts') %>
    <script src="../js/highlight.min.js"></script>
    <script>
      hljs.highlightAll();
    </script>
    <script src="../js/editFileController.js"></script>
  </body>
</html>
